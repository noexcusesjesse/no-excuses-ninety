[phases.setup]
# Node 22 — no native compile step (Postgres client is pure JS).
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build", "npm run db:migrate"]

[start]
cmd = "npm start"
